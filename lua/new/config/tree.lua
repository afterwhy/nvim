require("nvim-tree").setup({
    disable_netrw = false,
    hijack_netrw = false,

    view = {
        width = 30,
        side = "left",
    },

    update_focused_file = {
        enable = true,
    },
})
